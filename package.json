{
  "name": "spatial-engine-monorepo",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm build && pnpm test && pnpm typecheck",
    "publish:all": "pnpm check && pnpm -r publish --access public --no-git-checks"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "tsup": "^8.1.0",
    "@types/node": "^20.14.2"
  }
}